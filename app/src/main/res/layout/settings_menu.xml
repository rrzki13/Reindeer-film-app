<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/container_setting_menu"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="55dp"
    android:background="@color/darken">

    <TextView
        android:id="@+id/menu_settings_title"
        android:textColor="@color/white"
        android:layout_toStartOf="@id/ic_menu_settings"
        android:layout_centerVertical="true"
        android:layout_marginStart="15dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_information"
        android:fontFamily="@font/roboto_regular"
        android:textSize="17sp"/>

    <ImageView
        android:id="@+id/ic_menu_settings"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_chevron_right"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="15dp"
        android:contentDescription="@string/app_name" />


</RelativeLayout>